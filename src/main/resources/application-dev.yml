server:
  port: 8080
spring:
  thymeleaf:
    cache: false
  output:
    ansi:
      enabled: always
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: root
    password: root1234
    driver-class-name: org.postgresql.Driver